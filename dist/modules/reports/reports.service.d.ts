import { EmployeesService } from '../employees/employees.service';
import { PayrollService } from '../payroll/payroll.service';
import { AdvancesService } from '../advances/advances.service';
import { SalaryStructuresService } from '../salary-structures/salary-structures.service';
import { NonPayableDaysService } from '../non-payable-days/non-payable-days.service';
import { Role } from '../../common/enums/role.enum';
export declare class ReportsService {
    private employeesService;
    private payrollService;
    private advancesService;
    private salaryStructuresService;
    private nonPayableDaysService;
    constructor(employeesService: EmployeesService, payrollService: PayrollService, advancesService: AdvancesService, salaryStructuresService: SalaryStructuresService, nonPayableDaysService: NonPayableDaysService);
    filename(reportType: string): string;
    private assertRole;
    private escapeCsv;
    private escapeCurrency;
    private canViewHr;
    private canViewFinance;
    getDashboardData(role: Role): Promise<any>;
    generatePayrollCsv(month: number, year: number, role: Role): Promise<string>;
    generateBankTransferCsv(month: number, year: number, role: Role): Promise<string>;
    generatePFCsv(month: number, year: number, role: Role): Promise<string>;
    generateTaxCsv(month: number, year: number, role: Role): Promise<string>;
    generateAdvancesCsv(role: Role): Promise<string>;
    generateSalaryComponentsCsv(role: Role): Promise<string>;
    generatePayrollSummaryCsv(role: Role): Promise<string>;
    generateEmployeeMasterCsv(role: Role): Promise<string>;
    generateNonPayableDaysCsv(role: Role): Promise<string>;
    generateJoiningExitCsv(role: Role): Promise<string>;
}
